{
    "configurations": [
        {
            "name": "Debug Server",
            "type": "node",
            "request": "launch",
            "skipFiles": ["<node_internals>/**"],
            "program": "${workspaceFolder}/tests/db-connection.cjs",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}